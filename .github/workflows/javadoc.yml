name: Generate and Deploy Javadoc

on:
  push:
    branches:
      - master # Promijenite u main ako vam se glavna grana tako zove

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kod
        uses: actions/checkout@v4

      - name: Postavi Java okruženje
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Generisi Javadoc
        run: |
          mvn javadoc:javadoc
          if [ ! -d "target/site/apidocs" ]; then
            echo "Upozorenje: Maven nije generisao Javadoc."
            mkdir -p target/site/apidocs
            echo "<h1>Projekat petWebRescue - Javadoc uskoro stize</h1><p>Dodajte Java klase u src/main/java.</p>" > target/site/apidocs/index.html
          fi

      - name: Postavi GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Otpremi Javadoc arhivu
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'target/site/apidocs'

      - name: Objavi na GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
